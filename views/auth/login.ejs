<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>로그인 - SchoolHub</title>

  <link rel="stylesheet" href="/css/common.css" />
  <link rel="stylesheet" href="/css/post-new.css" />
</head>
<body>
  <%- include("../partials/header", { school: null }) %>

  <main class="container">
    <a href="/" class="back-link">← 메인으로 돌아가기</a>

    <h1>로그인</h1>
    <p>로그인하면 글과 댓글 작성 시 회원 닉네임이 자동으로 사용됩니다.</p>

    <% if (error) { %>
      <div class="error-box"><%= error %></div>
    <% } %>
    <% if (typeof passwordReset !== "undefined" && passwordReset) { %>
      <div class="success-box">
        비밀번호가 변경되었습니다. 새 비밀번호로 로그인해주세요.
      </div>
    <% } %>

    <form action="/login" method="POST" class="post-form">
      <label>
        이메일
        <input
          type="email"
          name="email"
          autocomplete="username"
          value="<%= form.email %>"
          placeholder="example@email.com"
          required
        />
      </label>

      <label>
        비밀번호
        <input
          type="password"
          name="password"
          autocomplete="current-password"
          required
        />
      </label>

      <div class="auth-help-row">
        <a href="/forgot-password" class="auth-sub-link">
          비밀번호를 잊으셨나요?
        </a>
      </div>

      <button type="submit">로그인</button>
    </form>
  </main>

  <%- include("../partials/footer") %>
</body>
</html>